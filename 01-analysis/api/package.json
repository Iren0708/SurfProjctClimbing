{
  "name": "vertical-gym-api-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Клиентское API скалодрома «Вертикаль» — запись на групповые тренировки (мульти-файловый OpenAPI)",
  "scripts": {
    "lint": "redocly lint",
    "preview": "redocly preview-docs",
    "bundle": "for d in auth slots bookings profile instructors; do redocly bundle $d -o dist/$d.yaml; done"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.0"
  }
}
